{
  "name": "hookah-assistant",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && .venv/bin/uvicorn app.main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "dev:backend": "cd backend && uvicorn app.main:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "postgres": "docker compose up -d postgres"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
